# 假设我们有一个图，节点从0到3，边如下：
# 0 -> 1
# 1 -> 2
# 2 -> 0, 3
# 3 -> 3
graph = [
    [1],  # 节点0与节点1相连
    [2],  # 节点1与节点2相连
    [0, 3], # 节点2与节点0和节点3相连
    [3]   # 节点3与自身相连
]
# 遍历
count =0
for i in graph:
    print(f"{count}->",end="")
    print(",".join(map(str,i)))
    count+=1